# StyleMate — Core Logic Reliability Audit

**Three different kinds of fix are mixed together below — they need different solutions:**

- 🔧 **Model/coverage fix** — a genuinely fixable accuracy ceiling (better candidates, more categories)
- 🚫 **Not visually solvable** — no AI model, however good, can determine this from a photo alone; needs a different data source entirely (user input, or stop scoring it)
- 🐛 **Pure code bug** — no AI involved, just a logic error, cheapest to fix

---

## 🚨 Headline finding — the core match score itself is partly built on noise

**23% of the total match-score weight** (fabric 8% + season 5% + style tag 6% + embellishment 4%) relies on fields the source analysis found to be near-random or worse from FashionCLIP. **The 3 most reliable signals** (embedding 22% + fit 7% + silhouette 6%) only add up to 35% of the total. This is worth saying directly: the "match %" shown to users is currently a blend where nearly a quarter of the formula is closer to noise than signal.

---

## Table 1 — Tagging fields with direct zero-shot classification

| ID  | Field            | Options | Reliability  | Problem                                                                                                                                                                                                               | Fix type                                                                                               |
| --- | ---------------- | ------- | ------------ | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------ |
| T1  | `category`       | 7       | High         | None — this one works                                                                                                                                                                                                 | ✅ Fine as-is                                                                                          |
| T2  | `pattern`        | 4       | Medium       | Missing floral, plaid, polka dot, geometric, tie-dye — all get forced into "printed" or "solid"                                                                                                                       | 🔧 Expand candidate list                                                                               |
| T3  | `dominant_color` | 13      | Medium       | Missing burgundy, teal, olive, sage, lavender, peach, coral, mint, turquoise, maroon — FashionCLIP predicts "red" for burgundy, "green" for olive                                                                     | 🔧 Expand candidates, or replace with pixel-based extraction (already proposed in `second_feature.md`) |
| T4  | `occasion_tag`   | 6       | **Very Low** | Not visually determinable — a black blazer could be "office" or "party" with equal validity. Confidence sits near noise (~0.12-0.15), below the review threshold, so almost everything needs review but isn't flagged | 🚫 Not visually solvable — needs to be user-set or inferred from category+formality instead            |
| T5  | `season`         | 5       | Low          | Weak visual signal — a cotton t-shirt has no real season                                                                                                                                                              | 🚫 Mostly not visually solvable — only extreme cases (heavy coat = winter) are reliable                |
| T6  | `fabric_type`    | 8       | **Very Low** | Fundamentally not visually determinable — even a human can't reliably tell cotton vs. linen vs. synthetic from a photo. Denim is the one exception                                                                    | 🚫 Not visually solvable — needs user input or should stop being a scored signal                       |
| T7  | `fit_type`       | 4       | Medium       | Depends on garment draping — a loose item photographed flat may look "regular"                                                                                                                                        | 🔧 Partially fixable with better photo guidance at upload                                              |
| T8  | `sleeve_length`  | 5       | High         | None — clear visual cue, this one works                                                                                                                                                                               | ✅ Fine as-is                                                                                          |

## Table 2 — Fields handled outside the standard candidate-list method

| ID  | Field            | Method                                       | Problem                                                                                                                                         | Fix type                                                                                                  |
| --- | ---------------- | -------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------- |
| T9  | `subcategory`    | Two-stage zero-shot (group → specific label) | Subtle visual distinctions like "skinny" vs. "straight_leg" or "palazzo" vs. "trousers" are genuinely hard for the model                        | 🔧 Model/coverage fix — realistic ceiling, not a bug                                                      |
| T10 | `garment_length` | Single zero-shot, 7 options                  | Reasonable — length from hemline is a real visual cue                                                                                           | ✅ Working as intended                                                                                    |
| T11 | `embellishments` | 9 binary checks, threshold 0.15              | Threshold is barely above noise — "buttons" fires on every button-up shirt; seams/zippers/pockets trigger false positives                       | 🐛 Threshold is a pure config fix                                                                         |
| T12 | `style_tags`     | Multi-label, threshold 0.12                  | Extremely low threshold — a plain t-shirt will likely match "fitted" or "structured" at 0.12 purely by chance                                   | 🐛 Threshold is a pure config fix                                                                         |
| T13 | `target_gender`  | Dedicated 3-phrase classifier, margin 0.05   | FashionCLIP has no real concept of gendered clothing — results are close to random, and the margin is too small to mean anything even if it did | 🚫 Not visually solvable — should be user-set or treated as a soft filter, not a confident classification |

## Table 3 — Fields with no FashionCLIP path at all

| ID  | Field             | Current behavior            | Problem                                                                               | Fix type                                                             |
| --- | ----------------- | --------------------------- | ------------------------------------------------------------------------------------- | -------------------------------------------------------------------- |
| T14 | `brand`           | Only populated via Gemini   | Stays permanently NULL if `TAGGING_PROVIDER=fashion_clip` (the default, free path)    | 🚫 Not visually solvable for most items — needs user input           |
| T15 | `formality_score` | Derived from `occasion_tag` | Compounds T4's unreliability — an already-shaky value building on another shaky value | 🐛 + 🚫 combined — fixing T4 (make occasion user-set) fixes this too |

---

## Table 4 — Color matching structural issues

| ID  | Issue                                       | What's wrong                                                                                                                 | Consequence                                                                                                                              | Fix type                                                                        |
| --- | ------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------- |
| C1  | Color-count mismatch                        | FashionCLIP predicts from only 13 colors; the scoring table (`HSL_MAP`) supports ~50                                         | "Burgundy," "maroon," "crimson," and "cherry" all collapse to the same generic "red" before scoring ever happens                         | 🔧 Expand FashionCLIP's candidates, or switch to pixel-based extraction         |
| C2  | Fuzzy substring matching is order-dependent | `"red" in "wine red"` matches "red" before "wine" is ever checked, because dictionary order decides the winner               | Derived color names silently collapse to their generic parent, losing the nuance the color table was built for                           | 🐛 Pure code bug                                                                |
| C3  | Unmapped colors fail silently               | A color like "charcoal" or "taupe" not in the table returns `None`, and every pairing involving that item quietly scores 0.5 | Invisible — the item just seems to match everything moderately well, for no visible reason                                               | 🐛 Pure code bug — same silent-fallback pattern as Cluster B in `first_bugs.md` |
| C4  | Neutral detection is oversensitive          | Any color with saturation under 15 or lightness outside 10-90 is treated as neutral                                          | A genuinely colorful pastel blue can get treated as neutral and score 0.9 with everything, including colors it would actually clash with | 🐛 Pure code bug — threshold tuning                                             |
| C5  | Clash list is too small                     | Only 7 hardcoded clashing color pairs exist                                                                                  | Real, well-known clashes (navy+black, brown+black, gold+silver, red+pink, blue+purple) are never penalized                               | 🔧 Content/coverage fix — needs real fashion-rule input, not just code          |

---

## Table 5 — Scoring signal reliability (the 9 weighted components)

| ID  | Signal        | Weight | False-positive risk                                                                                                                 | False-negative risk                                                                                    | Root cause                                                                              |
| --- | ------------- | ------ | ----------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------ | --------------------------------------------------------------------------------------- |
| S1  | Color         | 30%    | Oversensitive neutral detection (C4) scores real clashes as 0.9                                                                     | Navy+black never penalized — not in the clash list (C5)                                                | Table 4 issues                                                                          |
| S2  | Embedding     | 22%    | Two visually near-identical items (e.g. two white t-shirts) score as a near-perfect "match" despite being the same item type        | Genuinely good pairings with visually different embeddings (silk blouse + wool trousers) can score low | Structural — embeddings measure visual similarity, not styling compatibility, by design |
| S3  | Hard rules    | 12%    | Two items both (mis)tagged `occasion_tag="casual"` score a perfect match — even if the tag itself was a coin flip                   | —                                                                                                      | Directly inherited from T4's unreliability                                              |
| S4  | Fabric        | 8%     | A hardcoded "good" pair (e.g. denim+leather) scores well even when it looks bad in practice — the table encodes one opinion as fact | A real clash (silk+denim) scores low even when it can look great — same issue, opposite direction      | Directly inherited from T6's unreliability, compounded by a subjective hardcoded table  |
| S5  | Fit           | 7%     | —                                                                                                                                   | Only 4 fit types and 16 combinations exist — "relaxed," "tapered," "skinny" aren't represented at all  | Coverage gap, not a reliability issue                                                   |
| S6  | Season        | 5%     | Two items both defaulting to "summer" (a common fallback guess) score as a confident match                                          | —                                                                                                      | Directly inherited from T5's unreliability                                              |
| S7  | Style tag     | 6%     | The 0.12 threshold (T12) lets noise through, incorrectly triggering body-type boosts                                                | Most items never clear 0.12 at all, so the signal mostly sits at neutral                               | Directly inherited from T12                                                             |
| S8  | Silhouette    | 6%     | Only two possible outputs (0.9 or 0.5) — a slightly-off pairing scores identically to a completely wrong one                        | Only covers top+bottom — an A-line dress + fitted jacket has no rule at all and defaults to 0.5        | Coverage gap — same root as H8 in `first_bugs.md`                                       |
| S9  | Embellishment | 4%     | Never actually penalizes — the floor is 0.50, so this signal can only help, never hurt, even when it should                         | —                                                                                                      | Design choice, not a bug — worth deciding if that's actually intended                   |

## Table 6 — Outfit-level bonus/penalty edge cases

| ID  | Issue                                            | What's wrong                                                                                                          | Consequence                                                                                                                                         | Fix type                                                                    |
| --- | ------------------------------------------------ | --------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------- |
| B1  | Complementary + analogous bonuses can both fire  | +0.10 and +0.12 are separate additive bonuses that can stack to +0.22 on the same outfit                              | An outfit can get double-credited for two overlapping definitions of "the colors work," inflating the score beyond what either bonus alone intended | 🐛 Pure code bug                                                            |
| B2  | Bonus is flat, not proportional to outfit size   | A 2-item outfit and a 5-item outfit get the exact same flat bonus                                                     | Smaller outfits are disproportionately boosted relative to larger ones                                                                              | 🐛 Pure code bug                                                            |
| B3  | Busy-pattern penalty is uniform                  | Any 2+ busy patterns trigger the same flat 50% score cut, regardless of which patterns                                | Striped+floral (a real clash) gets treated identically to plaid+checked (which might work)                                                          | 🔧 Needs real fashion-rule nuance, not just a formula fix                   |
| B4  | All-neutral palette is detected but never scored | The code identifies an all-neutral outfit and adds a note to the explanation text, but the score itself is unaffected | The detection work is real but currently has zero functional impact                                                                                 | 🐛 Pure code bug — either wire it into scoring or remove the dead detection |

---

## Suggested fix order

1. **Fix the pure code bugs first (C2, C3, C4, B1, B2, B4)** — no AI tradeoffs, no design decisions, just correcting logic that's already wrong on its own terms. Cheapest, safest, highest confidence.
2. **Decide the "not visually solvable" fields (T4 occasion, T6 fabric, T13 gender)** — these need a product decision, not more engineering: either make them user-set at upload (a quick form field), or stop presenting them as confident AI output and reduce their scoring weight accordingly. Trying to fix these with a "better model" will not work — the source analysis is explicit that even humans can't do this from a photo.
3. **Apply the pixel-based color extraction already planned in `second_feature.md`** — this single fix resolves C1 and significantly reduces S1's false-positive risk at the same time.
4. **Rebalance the 9 scoring weights** once 1-3 are done — right now 23% of the formula leans on fields being actively improved or reduced; revisit the weight split after those changes land rather than before.
5. **Coverage gaps (T2, T9, C5, S5, S8)** — real but lower urgency; these are "the feature works but isn't comprehensive yet" issues, closer in spirit to `second_feature.md` than urgent fixes.
