# Backend Bug Audit

## Critical

| #   | Check | File                             | Line(s) | Description                                                                                                                                                                                             | Impact                                                                                       | Severity     |
| --- | ----- | -------------------------------- | ------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------- | ------------ |
| C1  | [x]   | `server/app/routers/wardrobe.py` | 1-64    | Imports nonexistent `WardrobeItem` model (not in `models.py`) and undefined schemas `WardrobeItemCreate`/`Update`/`Response` (not in `schemas.py`). Whole file is dead code — not mounted in `main.py`. | Would cause `ImportError` if ever mounted. Currently inert but blocks any future activation. | **Critical** |

## High

| #   | Check | File                                                            | Line(s)      | Description                                                                                                                                                            | Impact                                                                                                                                                                  | Severity |
| --- | ----- | --------------------------------------------------------------- | ------------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------- |
| H1  | [ ]   | `server/app/routers/calendar.py`                                | 151-168      | `link_try_on_image` auto-creates a `CalendarEntry` with hardcoded `user_id=1` when the requested entry_id doesn't exist. No authorization check.                       | Any user creates calendar entries attributed to demo user (id=1). Data attribution bug + privilege escalation.                                                          | **High** |
| H2  | [ ]   | `server/app/services/weather_service.py`                        | 49           | `rule_for_temp` fallback returns `TEMP_RULES[-1][1]` which is the integer `8` (max temperature of last rule), not a rule dict.                                         | `filter_items_by_weather` crashes with `TypeError` on temperatures >= 60°C or < -50°C (extreme but possible with bad API data).                                         | **High** |
| H3  | [ ]   | `server/app/routers/tagging.py` + `app/app/(tabs)/add-item.tsx` | 131 / 46     | `FORMALITY_MAP` duplicated across backend (Python) and frontend (TypeScript) with only a TODO comment to unify.                                                        | Occasion-to-formality scoring drifts silently if one copy is updated without the other. Frontend and backend disagree on which occasions map to which formality scores. | **High** |
| H4  | [ ]   | `server/app/pairing_engine.py`                                  | 860, 870     | Reason string `"balanced silhouette"` emitted for both `fit_score` (line 860) and `silhouette_score` (line 870) in `_score_pair_uncached`.                             | Users see duplicate identical fragments in outfit explanations.                                                                                                         | **High** |
| H5  | [ ]   | `server/app/recommender.py`                                     | 246-247, 260 | `get_recommendation_score` silently returns 0.5 when model file is missing OR when user/combo not found in the trained model.                                          | No logging or warning — recommender silently returns neutral scores as if the model works.                                                                              | **High** |
| H6  | [ ]   | `server/app/style_embeddings.py`                                | 377-408      | `compute_and_store_embedding` calls `db.commit()` in a background thread. Exception after commit prevents any notification to caller.                                  | Race condition: a new ClothingItem POST responds before embedding is fully stored. Subsequent outfit request sees no embedding → scores the item at neutral 0.5.        | **High** |
| H7  | [ ]   | `server/app/try_on_service.py`                                  | 1015-1058    | `generate_tryon()` function is dead code — never called. `render_outfit()` in the same file is the actual entry point.                                                 | Separate cache logic, photo hashing, and entry path are untested. If someone later uses this function expecting caching to work, they'll get wrong behavior.            | **High** |
| H8  | [ ]   | `server/app/pairing_engine.py`                                  | 512-545      | `_silhouette_balance_score` only handles `top` + `bottom` category pairs. Ethnic wear categories (kurti, saree, lehenga) acting as bottom-equivalents get neutral 0.5. | Users with Indian ethnic wear never get silhouette balance scores for top+kurti/saree combinations, even though SILHOUETTE_RULES supports the subcategories.            | **High** |

## Medium

| #   | Check | File                                                        | Line(s)           | Description                                                                                                                                                   | Impact                                                                                                                                           | Severity   |
| --- | ----- | ----------------------------------------------------------- | ----------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------ | ---------- |
| M1  | [ ]   | `server/app/pairing_engine.py`                              | 362               | `_pick_color_for_gap` returns hardcoded `"beige"` when no hues can be computed.                                                                               | Users always get "beige" recommended when their wardrobe colors are unrecognizable — no cycling through other neutrals.                          | **Medium** |
| M2  | [ ]   | `server/app/pairing_engine.py`                              | 988-1009          | `_outfit_body_type_boost` iterates `.get(bt, [])`. Valid body types with empty rule lists yield 0.0 boost with no warning.                                    | No style recommendations for valid body types that have empty rule entries, instead of a sensible default.                                       | **Medium** |
| M3  | [ ]   | `server/app/style_match.py`                                 | 848               | Hardcodes `"olive green"` in recommendations list, but `HSL_MAP` only has `"olive"` (no "olive green").                                                       | Frontend displays "olive green" but `_color_to_hsl("olive green")` returns None — can't compute color scores or produce shopping queries for it. | **Medium** |
| M4  | [ ]   | `server/app/pairing_engine.py`                              | 191-195           | Fuzzy color match `if key in c or c in key` iterates dict in insertion order. Ambiguous inputs map unpredictably — first match wins, not best semantic match. | A user input like "light blue" could match "blue" or "sky blue" depending on iteration order, not semantic distance.                             | **Medium** |
| M5  | [ ]   | `server/app/routers/outfits.py`                             | 196-209           | `create_outfit_feedback` accepts `user_id` from request body with zero validation — no check user exists or items belong to that user.                        | Malicious caller can poison LightFM training data with fabricated feedback for arbitrary users.                                                  | **Medium** |
| M6  | [ ]   | `server/app/models.py`                                      | 45, 51            | Both `formality` (String) and `formality_score` (Integer) stored in DB, derived from different pipelines (tagging vs clothing router).                        | If one field is updated independently (e.g. via API edit), the two can disagree. Item could have `formality_score=5` but `formality="casual"`.   | **Medium** |
| M7  | [ ]   | `server/app/routers/clothing.py`                            | 84, 94            | `update_item` and `delete_item` call `invalidate_item(db, item_id)` which is redundant for delete (FK cascade handles it).                                    | No consequence — just dead cleanup.                                                                                                              | **Medium** |
| M8  | [ ]   | `server/app/routers/tagging.py`                             | 482-496           | Color sanity check uses `confidence.get("dominant_color", 1.0) < 0.35`. Default of 1.0 means check is silently skipped if key missing.                        | Defensive code masks real confidence-writing failures.                                                                                           | **Medium** |
| M9  | [ ]   | `server/app/pairing_engine.py` + `server/app/pair_cache.py` | 1098-1118 / 61-72 | `build_capsule` queries all user items, then calls `ensure_pair_scores` which re-queries all items internally.                                                | Double query of identical data. Wasteful but not incorrect.                                                                                      | **Medium** |
| M10 | [ ]   | `server/app/services/packing_service.py`                    | 110-119           | `generate_packing_list` calls `gemini_json` synchronously (blocks up to 60s). Called from synchronous FastAPI endpoint `/packing/packing-list`.               | Blocks the entire ASGI worker thread for up to 60 seconds while waiting for Gemini.                                                              | **Medium** |
| M11 | [ ]   | `server/app/routers/shopping.py`                            | 86-89             | `shopping_suggestions` raises `HTTPException(502)` from catch-all exception handler on `provider.search()`, including transient network errors.               | Transient provider failure produces 502 error instead of gracefully returning partial (empty) results for that provider.                         | **Medium** |
| M12 | [ ]   | `server/app/style_match.py`                                 | 1016-1017         | `already_owned` AND `wardrobe_matches` both reference the exact same list object.                                                                             | Frontend receives identical data twice in the response payload.                                                                                  | **Medium** |

## Low

| #   | Check | File                                            | Line(s) | Description                                                                                                                                            | Impact                                                                                        | Severity |
| --- | ----- | ----------------------------------------------- | ------- | ------------------------------------------------------------------------------------------------------------------------------------------------------ | --------------------------------------------------------------------------------------------- | -------- |
| L1  | [ ]   | `server/app/pairing_engine.py`                  | 636-638 | `_embedding_similarity` returns 0.5 when either item lacks an embedding, with no logging.                                                              | Hard to diagnose why outfit scores are low — items may simply have no embeddings computed.    | **Low**  |
| L2  | [ ]   | `server/app/pairing_engine.py`                  | 452-460 | `_fabric_score` returns 0.5 when fabric data is None/missing.                                                                                          | No indication that fabric data is missing.                                                    | **Low**  |
| L3  | [ ]   | `server/app/pairing_engine.py`                  | 463-467 | `_fit_contrast_score` falls through to `dict.get(key, 0.5)` for any unmapped fit combination.                                                          | Users never see a warning that their fit combination has no scoring rule.                     | **Low**  |
| L4  | [ ]   | `server/app/main.py`                            | 36-37   | `Base.metadata.create_all` guarded by `engine.dialect.name == "sqlite"`. Postgres relies on alembic — no alembic setup exists.                         | Developer using Postgres finds tables missing and app crashes with relation-not-found errors. | **Low**  |
| L5  | [ ]   | `server/app/pairing_engine.py`                  | 921     | `score_outfit` returns 0.0 for empty input but 0.5 for single-item outfits. Inconsistent baseline.                                                     | Edge case — unlikely in normal use.                                                           | **Low**  |
| L6  | [ ]   | `server/app/pairing_engine.py`                  | 186-188 | `_color_to_hsl` doesn't normalize internal whitespace. `"  dark  blue  "` becomes `"dark  blue"` (double space preserved).                             | Doesn't cause failures (fuzzy fallback still matches) but unnecessary.                        | **Low**  |
| L7  | [ ]   | `server/app/routers/tagging.py`                 | 567-569 | `_tag_item` falls through to Gemini for ANY `TAGGING_PROVIDER` that isn't `"fashion_clip"`. A typo like `"fasion_clip"` silently hits paid Gemini API. | Costly mistake if env var is mistyped. Should validate known providers and raise.             | **Low**  |
| L8  | [ ]   | `server/app/style_advisor.py`                   | 211     | `explain_outfit` accesses `items[0].user` (lazy-loaded relationship), triggering an extra SQL query.                                                   | N+1 query on every explain-outfit call.                                                       | **Low**  |
| L9  | [ ]   | `server/app/services/fashion_rating_service.py` | 78      | `rate_outfit_photo` uses photo URL as TTL cache key. Same URL from different users returns same cached rating.                                         | Two users with the same photo URL (shared placeholder) get identical style ratings. Unlikely. | **Low**  |
| L10 | [ ]   | `server/app/routers/upload.py`                  | 31-33   | `upload_image` saves file then returns URL. Caller then tags the item via separate POST. If tagging is retried, the same file is re-uploaded.          | Duplicate files accumulate in storage on retry. Costs money on S3/Supabase.                   | **Low**  |

## Summary

| Severity     | Count  |
| ------------ | ------ |
| **Critical** | 1      |
| **High**     | 8      |
| **Medium**   | 12     |
| **Low**      | 10     |
| **Total**    | **31** |
